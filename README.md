# ResuME

ResuME is split into two apps:

- `backend`: FastAPI app managed with `uv`
- `frontend`: React app built with Vite and Tailwind CSS

## Backend

```sh
cd backend
uv sync
uv run fastapi dev app/main.py
```

## Frontend

```sh
cd frontend
npm install
npm run dev
```
